CREATE TABLE story (
    id integer NOT NULL AUTO_INCREMENT PRIMARY KEY,
    name varchar(50),
    position varchar(50),
    major varchar(50),
    year integer,
    program varchar(50),
    description varchar(500)
);